{
  "name": "koco-toastr",
  "version": "1.0.0",
  "homepage": "https://github.com/cbcrc/koco-toastr",
  "authors": [
    "Mark Moyes <mark.moyes@cbc.ca>"
  ],
  "description": "koco component for toastr ui",
  "main": "src/koco-toastr-ui.js",
  "keywords": [
    "koco",
    "knockout",
    "toastr"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "license": "MIT",
  "dependencies": {
    "toastr": "^2.1.1",
    "knockout": "^3.3.0"
  }
}
